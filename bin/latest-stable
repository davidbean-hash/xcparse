#!/usr/bin/env bash

set -euo pipefail

REPO="ChargePoint/xcparse"

curl --silent --fail "https://api.github.com/repos/${REPO}/releases/latest" \
  | grep -oE '"tag_name": *"[^"]+"' \
  | sed 's/"tag_name": *"//;s/"//'
